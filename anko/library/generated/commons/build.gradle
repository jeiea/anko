apply from: '../generated.gradle'

dependencies {
    embedded project(path: ':static:commons', configuration: 'default')
}

task androidReleaseSources(type: Jar, dependsOn: 'assembleRelease') {
    from("src", "../../static/commons/src")
}